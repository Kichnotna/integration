spring:
  application:
    name: integration_2
  datasource:
    url: jdbc:postgresql://localhost:5432/flyway2
    username: user
    password: password
  flyway:
    enabled: true
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

server:
  port: 8081
